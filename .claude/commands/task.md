---
description: Add a task (e.g. /task email Danish about the panel by Friday)
---
Add this to `$CONTEXT_DIR/tasks.md`: **$ARGUMENTS**

- Assign the **next free ID** from the file header, and update that counter.
- Put it in the right section. If it has a date, it goes in **NOW** with the due date.
- Write a short "why it matters" note if the stake isn't obvious — a bare imperative is a task Roman skips.
- Confirm in one line. Don't re-render the whole list unless I ask.
